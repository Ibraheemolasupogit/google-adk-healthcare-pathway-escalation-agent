# Screenshot Plan

Use `SYN-CANCER-62-003` and `mock-mcp` mode unless a screenshot specifically says otherwise.

## 1. Project Overview

- UI section: Overview.
- Fields visible: title, subtitle, safety banner, badges, default execution mode.
- Crop: full header plus first paragraph.
- Caption: "Synthetic Google ADK pathway review demo with MCP and human review."
- Must not show: terminal, local paths, browser bookmarks with private data.

## 2. Synthetic Case Selection

- UI section: Select Case.
- Selected case: `SYN-CANCER-62-003`.
- Fields visible: case ID, pathway, current stage, referral time, assessment time, priority, flags, synthetic indicator.
- Crop: case JSON area.
- Caption: "Prebuilt synthetic Cancer 62-day case selected for demonstration."
- Must not show: any free-text patient entry box, because none should exist.

## 3. Deterministic Assessment

- UI section: Deterministic Assessment.
- Fields visible: breach status `BREACHED`, risk score `7.5`, risk level `CRITICAL`, target consumption, audit trace ID.
- Crop: metric row plus first structured JSON block.
- Caption: "Deterministic breach and risk calculation preserved as source of truth."
- Must not show: claims that values are generated by Gemini.

## 4. Agent Workflow and MCP Invocations

- UI section: Agent Workflow.
- Execution mode: `mock-mcp`.
- Fields visible: workflow sequence, execution mode, tool or MCP invocation table.
- Crop: workflow and invocation table.
- Caption: "Google ADK workflow using local MCP-compatible capabilities."
- Must not show: hidden reasoning or chain of thought.

## 5. Security Guardrails

- UI section: Security and Guardrails.
- Fields visible: guardrail status, safe-for-human-review, deterministic integrity statement, findings table.
- Crop: guardrail JSON and findings table.
- Caption: "Guardrails check safety and deterministic integrity before review."
- Must not show: a claim of perfect security.

## 6. Human-Review Result

- UI section: Human Review.
- Fields visible: review ID, status, `submitted=false`, `authenticated_identity=false`, hashes and history.
- Crop: review record summary.
- Caption: "Human review is explicit and remains demonstration-only."
- Must not show: real reviewer identity or claimed authentication.

## 7. Evaluation Results

- UI section: Evaluation Evidence.
- Fields visible: run ID, benchmark version, 30 cases, pathway distribution, overall pass rate, metric rows.
- Crop: benchmark summary and top metric rows.
- Caption: "Controlled synthetic benchmark evidence committed with the repository."
- Must not show: production performance claims.

## 8. Architecture

- UI section: Architecture and Limitations.
- Fields visible: User -> agents -> Human Reviewer flow, architecture JSON summary, limitations.
- Crop: workflow and limitations.
- Caption: "Boundaries keep deterministic logic, agents, MCP, guardrails and review separate."
- Must not show: claims of live NHS integration.

