# MCP Local Development

Local stdio entry points:

```bash
python3 -m mcp_servers.case_data_server
python3 -m mcp_servers.pathway_rules_server
python3 -m mcp_servers.policy_evidence_server
```

For tests and CLI demos, use `mock-mcp` mode or the `mcp-*` CLI commands. These do not bind public ports.
